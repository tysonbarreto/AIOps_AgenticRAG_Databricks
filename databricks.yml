# This is a Databricks asset bundle definition for aiops_rag_databricksapp.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: aiops_rag_databricksapp
  uuid: 8e83dfd7-e406-4b52-8dc9-211fc2809800

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-47b1e6a2-1d5b.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-47b1e6a2-1d5b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/tysonbarretto1991@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/tysonbarretto1991@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: tysonbarretto1991@gmail.com
        level: CAN_MANAGE
